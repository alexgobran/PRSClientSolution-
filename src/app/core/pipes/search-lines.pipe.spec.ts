import { SearchLinesPipe } from './search-lines.pipe';

describe('SearchLinesPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchLinesPipe();
    expect(pipe).toBeTruthy();
  });
});
